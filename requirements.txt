# Court-visibility gate — runtime deps.
# Verified present in /opt/anaconda3 (torch 2.2.2, MPS available; cv2 4.13.0).
# open_clip is intentionally NOT required: CLIP ViT-B/32 is loaded via transformers.
torch>=2.2
transformers>=4.38
scikit-learn>=1.3
opencv-python>=4.8
pillow>=10.0
numpy>=1.24
matplotlib>=3.7
joblib>=1.3
tqdm>=4.65
